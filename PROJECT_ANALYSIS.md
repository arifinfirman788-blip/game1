# 项目分析报告：黄小西带你游贵州 — 三消 H5 游戏

## 一、项目概览

| 项目 | 说明 |
|------|------|
| 项目名称 | guizhou-match3-h5 |
| 产品名称 | 黄小西带你游贵州 |
| 版本 | 0.1.0 |
| 项目类型 | 移动端 H5 三消游戏 |
| 目标平台 | 移动端浏览器 / 微信小程序 WebView |
| 设计稿宽度 | 393px（适配 iPhone 14/15） |
| 最大显示宽度 | 430px（桌面端居中展示） |

---

## 二、技术栈

| 分类 | 技术 | 版本/说明 |
|------|------|-----------|
| 前端框架 | Vue 3 | 3.2.47（Composition API + `<script setup>`） |
| 构建工具 | Vite | 5.4.19 |
| UI 组件库 | Vant | 4.9.20（移动端组件：Button/Icon/Progress/Badge/Toast/Dialog） |
| CSS 适配 | postcss-mobile-forever | 5.0.0（设计稿 393px，px 自动转 vw） |
| 图片优化 | vite-plugin-imagemin | 支持 mozjpeg/pngquant/webp/svgo/gifsicle |
| 部署 | GitHub Pages | GitHub Actions 自动构建部署，basePath: `/game1/` |
| 包管理 | npm | ESM 模块（`"type": "module"`） |

---

## 三、项目结构

```
game/
├── .github/workflows/
│   └── deploy.yml                 # GitHub Actions CI/CD 配置
├── image/                         # 源图片资源（卡牌、结算等）
│   ├── card-blue.png
│   ├── card-purple.png
│   ├── card-gold.png
│   └── 结算界面*.png / 失败*.png
├── public/assets/                 # 静态资源目录（Vite 原样拷贝）
│   ├── home-bg-new.jpg            # 首页背景
│   ├── custom-bg.png              # 游戏默认背景
│   ├── detail/                    # 游戏详情页资源
│   │   ├── backgrounds/           # 章节专属背景
│   │   ├── boosters/              # 道具图标（锤子、宝瓶、银花、面具、手掌）
│   │   ├── characters/            # 角色（黄小西）
│   │   ├── pieces/                # 棋子素材（6种美食）
│   │   ├── specials/              # 特殊棋子特效
│   │   └── ui/                    # 游戏UI素材（棋盘、宝箱、星星等）
│   ├── map/                       # 地图页资源
│   │   ├── mobile/                # 移动端专用资源
│   │   ├── chapter-highlights/    # 章节美食展示
│   │   ├── nodes/                 # 关卡节点图标（当前/锁定/0-3星）
│   │   ├── chests/                # 宝箱图标
│   │   └── buttons/               # 按钮图标
│   └── settle/                    # 结算页资源
├── src/
│   ├── main.js                    # 应用入口，注册 Vue + Vant
│   ├── App.vue                    # 主组件（1117行，核心页面路由+游戏逻辑桥接）
│   ├── styles.css                 # 全局样式（3132行）
│   ├── components/                # 组件
│   │   ├── LevelSettle.vue        # 通关结算页（含抽卡）
│   │   ├── CardDraw.vue           # 抽卡动画组件
│   │   ├── CardInventory.vue      # 卡牌背包（筛选+列表）
│   │   └── CardDetail.vue         # 卡牌详情+兑换
│   ├── composables/               # 组合式函数（核心业务逻辑）
│   │   ├── useMatch3Game.js       # 三消游戏引擎（1015行）
│   │   ├── usePlayerProgress.js   # 玩家进度+存档（174行）
│   │   └── useCardSystem.js       # 卡牌系统逻辑（150行）
│   └── config/                    # 配置
│       ├── levels.js              # 关卡配置+生成算法（124行）
│       ├── assets.js              # 资源清单映射（123行）
│       └── cardSystem.js          # 卡牌系统配置+Mock API（288行）
├── index.html                     # HTML 入口
├── vite.config.js                 # Vite 构建配置
├── package.json                   # 项目依赖
└── test_build.js                  # 构建测试脚本
```

**代码规模**：源码共 7,805 行（含 Vue 模板/脚本/样式、JS 逻辑、CSS）

---

## 四、业务功能

### 4.1 游戏核心玩法 — 三消（Match-3）

| 功能 | 说明 |
|------|------|
| 棋盘 | 8×8 网格，6 种贵州美食棋子（肠旺辣椒、蓝靛粑、丝娃娃、黄粑、玫瑰糖、酸汤鱼） |
| 交互 | 点击选中+点击交换 / 滑动交换 |
| 匹配 | 横/纵向三连及以上自动消除，支持连锁消除（combo） |
| 特殊棋子 | 4 连 → 光晕（横/竖排消除）、复合 → 炸弹（九宫格消除）、5 连 → 彩虹球（全屏同类消除） |
| 特殊组合 | 彩虹+彩虹 = 清全屏、彩虹+特殊 = 全屏同类变异 |
| 障碍物 | 冰块（第11关解锁）、木箱（第26关解锁，阻止棋子下落）、银链（第41关解锁） |
| 关卡目标 | 收集指定数量棋子/消除指定数量障碍物 |
| 步数限制 | 16~30 步（随难度调整） |
| 星级评价 | 1~3 星（基于得分门槛，自动计算） |
| 剩余步数结算 | 通关后自动消耗剩余步数（优先释放特殊棋子，再随机匹配） |

### 4.2 关卡系统

| 功能 | 说明 |
|------|------|
| 章节结构 | 7 大章 × 100 关 = 700 关 |
| 章节主题 | 黄果树瀑布、荔波小七孔、西江苗寨、镇远古镇、赤水丹霞、万峰林、梵净山 |
| 难度递增 | 章节+关卡双重难度叠加：`difficulty = chapterIndex × 0.42 + localLevel / 100` |
| 障碍物成长 | 冰块 4~18、木箱 3~15、银链 2~13，按关卡线性增长 + 章节加成 |
| 目标生成 | 1~3 个目标（关卡越高目标越多），数量随难度递增 |
| 步数预算 | `25 + 前期宽松 + 障碍物补偿 - 关卡压力`，clamp(16, 30) |
| 星级门槛 | 动态计算：1 星 = base, 2 星 = base×1.9, 3 星 = base×3 |

### 4.3 道具系统

| 道具 | 名称 | 效果 |
|------|------|------|
| hammer | 银锤 | 直接敲开一个格子（清除棋子/障碍物） |
| bottle | 蜡染宝瓶 | 收集棋盘上全部同类棋子 |
| flower | 银花（绣球） | 清除九宫格范围 |
| mask | 傩戏面具 | 将一个棋子变为彩虹球 |
| hand | 银饰手掌 | 将一个棋子变为炸弹 |

道具来源：宝箱奖励（关卡 3/10/16/20 等节点领取）

### 4.4 地图选关系统

| 功能 | 说明 |
|------|------|
| 章节切换 | 7 大章 Tab 切换，展示章节美食/景点信息 |
| 路线地图 | 蛇形路线排列关卡节点，5 页分页展示（每页 20 关） |
| 滑动翻页 | 水平滑动切换地图页，未解锁页不可翻 |
| 关卡节点 | 显示关卡编号、锁定/当前/已通关状态、0~3 星回显 |
| 宝箱节点 | 在路线中间位置显示宝箱（关闭/可领取/已开启） |
| 进度统计 | 章节星级进度条（总星/300） |

### 4.5 卡牌系统

| 功能 | 说明 |
|------|------|
| 抽卡 | 通关结算后从 3 张背面卡牌中选 1 张翻转揭示 |
| 卡牌等级 | R（普通/蓝，60%）、SR（稀有/紫，30%）、SSR（史诗/金，9%）、UR（传说/红，1%） |
| 卡牌分类 | 景区 / 文化 / 美食 |
| 卡牌背包 | 按等级/类型/地区三级筛选，网格展示 |
| 卡牌详情 | 展示卡牌信息+关联权益 |
| 权益兑换 | 生成兑换码，支持跳转微信小程序核销 |
| 数据持久化 | localStorage 存储卡牌背包 |

### 4.6 结算系统

| 功能 | 说明 |
|------|------|
| 通关结算 | 3 阶段动画：横幅 → 抽卡 → 详情面板（星级+连击+步数奖励） |
| 失败结算 | 弹窗展示最终得分 + 再来一次/返回选关 |
| 分数动画 | 缓出计数器动画展示最终得分 |
| 抽卡嵌入 | 结算流程内嵌 CardDraw 组件，抽卡后可查看卡牌详情 |

---

## 五、技术架构

### 5.1 整体架构

```
┌─────────────────────────────────────────────────────────┐
│                      App.vue                            │
│  ┌──────────┐  ┌──────────┐  ┌──────────────────────┐  │
│  │  首页     │  │  地图页   │  │      游戏页          │  │
│  │  (home)  │  │  (map)   │  │      (game)          │  │
│  └──────────┘  └──────────┘  │  ┌────────────────┐  │  │
│                              │  │ LevelSettle    │  │  │
│  ┌───────────────────────────│  │  └ CardDraw    │  │  │
│  │ CardInventory (全屏覆盖)  │  └────────────────┘  │  │
│  │ CardDetail   (全屏弹窗)   │  ┌────────────────┐  │  │
│  └───────────────────────────│  │ 失败结算弹窗    │  │  │
│                              │  └────────────────┘  │  │
│                              └──────────────────────┘  │
└─────────────────────────────────────────────────────────┘
          │                 │                │
    ┌─────┴─────┐    ┌─────┴─────┐   ┌─────┴──────┐
    │ usePlayer │    │ useMatch3 │   │ useCard    │
    │ Progress  │    │ Game      │   │ System     │
    │ (存档/进度)│    │ (游戏引擎)│   │ (卡牌逻辑) │
    └─────┬─────┘    └─────┬─────┘   └─────┬──────┘
          │                │                │
    ┌─────┴─────┐    ┌─────┴─────┐   ┌─────┴──────┐
    │ localStorage   │ levels.js │   │ cardSystem │
    │ (持久化)  │    │ (关卡配置)│   │ (卡牌配置) │
    └───────────┘    └───────────┘   └────────────┘
```

### 5.2 页面路由机制

项目采用**状态驱动**的页面切换（非 vue-router），通过 `screen` ref 控制：

- `screen === 'home'` → 首页
- `screen === 'map'` → 地图选关
- `screen === 'game'` → 游戏页面

URL 参数同步：
- `?level=128` → 直接进入游戏页
- `?screen=map` → 直接进入地图页
- 默认 → 进入首页

### 5.3 核心模块详解

#### useMatch3Game（游戏引擎）

纯前端三消引擎，无服务端依赖，核心流程：

```
初始化棋盘 → 玩家交换 → 校验匹配 → 消除+特殊棋子生成
    ↑                                    │
    └── 重力下落+补充 ← 结算得分+连击 ←─┘
```

关键设计：
- **reactive 响应式状态**：board/selected/goals/effects 全部响应式，驱动 UI 更新
- **异步动画编排**：通过 `delay()` + `await nextTick()` 实现消除→下落→补充的动画序列
- **特效系统**：effects 对象管理清除高亮、交换位移、下落偏移、粒子、光环、飘字等视觉反馈
- **自动消除**：通关后 `autoPlayRemainingMoves()` 循环触发特殊棋子 → 随机匹配，直到步数耗尽
- **无死局检测**：暂无棋盘重洗机制

#### usePlayerProgress（玩家存档）

- 数据存储于 `localStorage`（key: `guizhou-match3-progress`）
- 版本化迁移（当前 v2），低版本数据自动合并默认值
- 深度 watch 自动持久化
- 管理内容：关卡解锁、星级记录、宝箱领取、道具背包

#### useCardSystem（卡牌系统）

- 卡牌数据存储于 `localStorage`（key: `guizhou-card-inventory`）
- 当前为 Mock API，预留后端接口注释
- 提供抽卡/兑换/加载三大核心操作

### 5.4 关卡生成算法

采用**程序化生成**而非手工配置，700 关全部由算法生成：

```
输入: globalLevel (1-700)
  → chapterIndex = floor((level-1) / 100)
  → localLevel = ((level-1) % 100) + 1
  → difficulty = chapterIndex × 0.42 + localLevel / 100
  → blockerPlan = 按 blockerRules 生成障碍物
  → goals = 基于 primaryPiece + 障碍物 + difficulty
  → moves = clamp(25 + 宽松 + 障碍物补偿 - 压力, 16, 30)
  → starTargets = 动态得分门槛
```

### 5.5 资源管理

- `assets.js` 统一管理所有资源路径，通过 `asset()` 辅助函数拼接 `BASE_URL`
- 支持 Vite 的 `import` 方式引入图片（如卡牌图片），确保打包路径正确
- Vite 插件自动优化：JPEG quality 80、PNG quantize、WebP 转换
- 图片懒加载：地图/棋子/道具使用 `loading="lazy" decoding="async"`

---

## 六、UI 设计体系

### 6.1 视觉风格

- **主题**：贵州文旅民族风 — 银饰+蜡染+苗绣
- **配色**：深靛蓝(#173956) + 金色(#f6bf3a) + 朱红(#b63225) + 翠绿(jade) + 米纸底色(#f5ead5)
- **字体**：PingFang SC / Microsoft YaHei / system-ui
- **布局**：移动端全屏沉浸，桌面端居中 430px 手机壳

### 6.2 CSS 架构

- 全局 CSS 变量定义设计 Token（颜色/阴影）
- `postcss-mobile-forever` 自动 px → vw 适配
- 3132 行全局样式 + 组件 scoped 样式
- 大量 CSS 动画：棋子消除/下落/粒子/抽卡翻转/结算分步揭示

---

## 七、部署与运维

| 项目 | 说明 |
|------|------|
| CI/CD | GitHub Actions，push main 触发自动构建部署 |
| 构建命令 | `VITE_BASE=/game1/ npm run build` |
| 部署目标 | GitHub Pages |
| Node 版本 | 24 |
| 缓存 | npm cache（setup-node 内置） |

---

## 八、当前开发状态与待办

### 已完成

- 三消核心玩法（棋盘、匹配、消除、特殊棋子、障碍物）
- 7 章 700 关程序化生成
- 地图选关 + 宝箱奖励
- 道具系统（5 种道具）
- 通关/失败结算流程
- 卡牌系统（抽卡/背包/详情/兑换）
- 首页 + 地图 + 游戏 三屏切换
- 移动端适配 + 图片优化
- GitHub Pages 自动部署

### 待完成 / TODO

| 项目 | 说明 | 位置 |
|------|------|------|
| 后端 API 接入 | 卡牌系统当前为 Mock，需替换为真实 API | `cardSystem.js` 多处 TODO |
| 死局检测 | 棋盘无可用移动时缺乏重洗机制 | `useMatch3Game.js` |
| 设置面板 | 音效/震动/账号等入口已预留但未实现 | `App.vue:openSettings` |
| 小程序跳转 | CardDetail 中 appId 为占位符 | `CardDetail.vue:handleJumpToMiniProgram` |
| 复制提示 | 复制兑换码后缺少 Toast 反馈 | `CardDetail.vue:handleCopyCode` |
| 音效系统 | 无音效实现 | — |
| 更多卡牌图片 | 当前蓝/紫/金/红 4 种卡牌共用模板图 | `image/` 目录 |
| 结算页 settle 资源 | 部分 settle 资源路径已定义但文件可能缺失 | `assets.js:settle` |

---

## 九、技术亮点

1. **纯前端游戏引擎**：三消核心逻辑完全客户端运行，无需服务端，700 关程序化生成零配置
2. **响应式动画编排**：利用 Vue 3 reactive + async/await 实现流畅的消除动画链
3. **精细的移动端适配**：postcss-mobile-forever + viewport 高度动态计算 + 触摸手势
4. **模块化 Composable 架构**：游戏引擎/玩家存档/卡牌系统三者解耦，便于独立测试和替换
5. **卡牌系统预留后端**：Mock API 与真实 API 接口一一对应，迁移成本低
6. **文旅主题深度融合**：棋子/道具/章节/卡牌全部使用贵州文化元素命名（银锤/蜡染宝瓶/傩戏面具等）
